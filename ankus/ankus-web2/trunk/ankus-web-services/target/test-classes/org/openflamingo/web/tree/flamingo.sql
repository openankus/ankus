DROP TABLE IF EXISTS TREE;

CREATE TABLE TREE (
  ID          INT(11)      NOT NULL AUTO_INCREMENT COMMENT 'Sequence',
  NAME        VARCHAR(250)   NOT NULL COMMENT 'Name',
  TREE        VARCHAR(10)   NOT NULL COMMENT 'Tree Type',
  NODE        VARCHAR(10)  NOT NULL COMMENT 'Node Type',
  ROOT        BOOLEAN      DEFAULT NULL COMMENT 'Username',
  USERNAME    VARCHAR(50)  NOT NULL COMMENT 'Username',
  PARENT_ID   INT(11)      DEFAULT NULL COMMENT 'Parent',
  FOREIGN KEY(PARENT_ID) REFERENCES TREE(ID),
  PRIMARY KEY (ID)
)
  ENGINE = InnoDB
  DEFAULT CHARSET = UTF8;

INSERT INTO TREE (ID, NAME, TREE, NODE, ROOT, USERNAME)
  VALUES (1, '/', 'JOB', 'FOLDER', true, 'fharenheit');

INSERT INTO TREE (ID, NAME, TREE, NODE, ROOT, USERNAME)
  VALUES (2, '/', 'WORKFLOW', 'FOLDER', true, 'fharenheit');

INSERT INTO TREE (ID, NAME, TREE, NODE, ROOT, USERNAME)
  VALUES (3, '/', 'JOB', 'FOLDER', true, 'rachell');

INSERT INTO TREE (ID, NAME, TREE, NODE, ROOT, USERNAME)
  VALUES (4, '/', 'WORKFLOW', 'FOLDER', true, 'rachell');

INSERT INTO TREE (ID, NAME, TREE, NODE, ROOT, USERNAME, PARENT_ID)
  VALUES (5, 'test', 'WORKFLOW', 'ITEM', false, 'fharenheit', '2');

